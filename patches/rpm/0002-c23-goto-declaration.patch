diff --git a/lib/fsm.c b/lib/fsm.c
index cbfb62cbf..88e14f8a6 100644
--- a/lib/fsm.c
+++ b/lib/fsm.c
@@ -1024,7 +1024,7 @@ int rpmPackageFilesInstall(rpmts ts, rpmte te, rpmfiles files,
                     rc = RPMERR_UNKNOWN_FILETYPE;
             }
 
-setmeta:
+setmeta:;
 	    /* Special files require path-based ops */
 	    mayopen = S_ISREG(fp->sb.st_mode) || S_ISDIR(fp->sb.st_mode);
 	    if (!rc && fd == -1 && mayopen) {
